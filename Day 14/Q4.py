'''If we want to create empty set we should use "set()" and it stores values using {} 
this only creates empty dictionary and dictionary stores pairs of values'''
emptySet=set()#only one way to create empty set
emptyDict={}#If we use empty {} then we can create empty dictionary
print(emptySet,"and",type(emptySet))
print(emptyDict,"and",type(emptyDict))
