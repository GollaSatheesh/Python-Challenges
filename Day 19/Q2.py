'''Variable length arguments means if we dont exactly how much varables will be using in the 
   function then and we can pass any number of arguments in the function in Variable length 
   arguments.They are 2 types
      1.keyword variable lenth arguments 
          We can use this by "*Args* with this by passing in the parameters we can only define 
          single values not key value pairs in function definition
      2.Non-Keyword variable lenth arguments
          We can us this to pass key value pair arguments by using "**" Symbol'''
