'''If we want to store multiple objective elements in single entity then we can 
use set and it wont accept duplicates if we create duplicate it automatically 
considers it as a single object and it wont stores order of insertion it uses 
hash principle to store data'''

s={10,20,30,40,50}
print(s,"and",type(s))
